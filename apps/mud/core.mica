// Small relational world core for daemon/telnet demos.
//
// This file intentionally keeps the world model in ordinary Mica relations:
// rooms, exits, containment, visibility, names, descriptions, and command verbs
// are all authorable world state rather than privileged runtime concepts.

make_identity(:player)
make_identity(:programmer)
make_identity(:builder)
make_identity(:thing)
make_identity(:portable)
make_identity(:container)
make_identity(:room)
make_identity(:area)
make_identity(:passage)
make_identity(:event/movement)
make_identity(:event/arrival)
make_identity(:event/take)
make_identity(:event/take_from)
make_identity(:event/drop)
make_identity(:event/put)
make_identity(:event/say)
make_identity(:event/emote)
make_identity(:event/social)
make_identity(:event/look)
make_identity(:event/examine)
make_identity(:event/utility)
make_identity(:event/button)
make_identity(:event/log_entry)

make_identity(:alice)
make_identity(:coin)
make_identity(:box)
make_identity(:red_button)
make_identity(:first_room)
make_identity(:north_room)
make_identity(:attic)
make_identity(:hotel_area)
make_identity(:bob)

make_identity(:command/pattern/look_room)
make_identity(:command/pattern/look_target)
make_identity(:command/pattern/look_inside)
make_identity(:command/pattern/message_command)
make_identity(:command/pattern/get)
make_identity(:command/pattern/get_from)
make_identity(:command/pattern/drop)
make_identity(:command/pattern/put)
make_identity(:command/pattern/push)
make_identity(:command/pattern/say)
make_identity(:command/pattern/emote)
make_identity(:command/pattern/social_target)
make_identity(:command/pattern/social_at)
make_identity(:command/pattern/social_to)
make_identity(:command/pattern/social_none)
make_identity(:command/pattern/think)

make_relation(:Delegates, 3)

make_relation(:Portable, 1)
make_functional_relation(:Name, 2, [0])
make_functional_relation(:Description, 2, [0])
make_functional_relation(:LocatedIn, 2, [0])
make_functional_relation(:RoomInArea, 2, [0])
make_relation(:ProperNoun, 1)
make_relation(:Plural, 1)
make_functional_relation(:PronounSubject, 2, [0])
make_functional_relation(:PronounObject, 2, [0])
make_functional_relation(:PronounPossessiveAdjective, 2, [0])
make_functional_relation(:PronounPossessiveNoun, 2, [0])
make_functional_relation(:PronounReflexive, 2, [0])

make_relation(:PassageInArea, 2)
make_relation(:PassageSide, 3)
make_functional_relation(:PassageLabel, 3, [0, 1])
make_relation(:PassageAlias, 3)
make_functional_relation(:PassageDescription, 3, [0, 1])
make_functional_relation(:PassageAmbient, 3, [0, 1])
make_functional_relation(:PassageOpen, 2, [0])
make_relation(:Exit, 3)
make_relation(:ReachableRoom, 3)
make_relation(:Builder, 1)

// Recursive containment/location views.
make_relation(:Within, 2)
make_relation(:Contents, 2)
make_relation(:Carrying, 2)
make_relation(:CanSee, 2)
make_relation(:PresentActor, 2)
make_relation(:ButtonArmed, 1)

// Parser-facing state is separate from display state. A thing can be visible
// without being allowed to pollute the command parser.
make_relation(:command/VerbAlias, 2)
make_relation(:command/PrefixShortcut, 2)
make_relation(:command/Form, 1)
make_relation(:command/Pattern, 3)
make_relation(:command/PatternPart, 4)
make_relation(:command/Role, 3)
make_relation(:command/Preposition, 2)
make_functional_relation(:command/Summary, 2, [0])
make_functional_relation(:command/MissingArgumentMessage, 2, [0])
make_relation(:command/Noun, 2)
make_relation(:command/NounAlias, 2)
make_relation(:command/Name, 2)
make_relation(:VisibleTo, 2)
make_relation(:event/Delivery, 2)
make_functional_relation(:event/Seq, 2, [0])
make_functional_relation(:event/Text, 2, [0])
make_functional_relation(:event/Source, 2, [0])
make_relation(:event/NarrativeEntry, 4)
make_relation(:command/TrustedForName, 2)
make_relation(:command/NameMatchable, 2)
make_relation(:command/TrustedForGrammar, 2)
make_relation(:command/GrammarSource, 2)
make_relation(:command/HandlerScope, 2)

make_relation(:CanRead, 2)
make_relation(:CanWrite, 2)
make_relation(:CanInvoke, 2)
make_relation(:CanEffect, 1)
make_relation(:RoleCanRead, 2)
make_relation(:RoleCanWrite, 2)
make_relation(:RoleCanInvoke, 2)
make_relation(:RoleCanEffect, 1)

assert Delegates(#portable, #thing, 0)
assert Delegates(#container, #thing, 0)
assert Delegates(#player, #thing, 0)
assert Delegates(#alice, #programmer, 0)
assert Delegates(#alice, #builder, 0)
assert Delegates(#event/movement, #event, 0)
assert Delegates(#event/arrival, #event, 0)
assert Delegates(#event/take, #event, 0)
assert Delegates(#event/take_from, #event, 0)
assert Delegates(#event/drop, #event, 0)
assert Delegates(#event/put, #event, 0)
assert Delegates(#event/say, #event, 0)
assert Delegates(#event/emote, #event, 0)
assert Delegates(#event/social, #event, 0)
assert Delegates(#event/look, #event, 0)
assert Delegates(#event/examine, #event, 0)
assert Delegates(#event/utility, #event, 0)
assert Delegates(#event/button, #event, 0)
assert Delegates(#event/log_entry, #event, 0)
assert Delegates(#coin, #portable, 0)
assert Delegates(#alice, #player, 0)
assert Delegates(#bob, #player, 0)
assert Delegates(#box, #container, 0)
assert Delegates(#red_button, #thing, 0)
assert Delegates(#first_room, #room, 0)
assert Delegates(#north_room, #room, 0)
assert Delegates(#attic, #room, 0)
assert Delegates(#hotel_area, #area, 0)

assert Portable(#coin)

#alice.name = "Alice"
#bob.name = "Bob"
#coin.name = "coin"
#box.name = "box"
#red_button.name = "button"
#first_room.name = "First Room"
#north_room.name = "North Room"
#attic.name = "Dusty Attic"
#hotel_area.name = "Timbran Hotel"
assert ProperNoun(#alice)
assert ProperNoun(#bob)
assert PronounSubject(#alice, "she")
assert PronounObject(#alice, "her")
assert PronounPossessiveAdjective(#alice, "her")
assert PronounPossessiveNoun(#alice, "hers")
assert PronounReflexive(#alice, "herself")
assert PronounSubject(#bob, "he")
assert PronounObject(#bob, "him")
assert PronounPossessiveAdjective(#bob, "his")
assert PronounPossessiveNoun(#bob, "his")
assert PronounReflexive(#bob, "himself")

#alice.description = "Alice is alert and ready to test the room's stranger affordances."
#coin.description = "A tarnished brass coin catches the light."
#bob.description = "Bob is here, looking faintly puzzled."
#box.description = "A small wooden box rests here, open and empty."
#red_button.description = "A red button protrudes from the wall under a brass sign reading DO NOT PRESS."
#first_room.description = "First Room. You are standing in a plain stone room."
#north_room.description = "North Room. It is quieter here, and the ceiling slopes upward."
#attic.description = "Dusty Attic. Old beams vanish into shadow."
#hotel_area.description = "A development and demonstration area for the Timbran Hotel."

#alice.locatedIn = #first_room
#bob.locatedIn = #first_room
#coin.locatedIn = #first_room
#box.locatedIn = #first_room
#red_button.locatedIn = #first_room

#first_room.roomInArea = #hotel_area
#north_room.roomInArea = #hotel_area
#attic.roomInArea = #hotel_area

assert Builder(#alice)

assert PassageInArea(#passage<[#first_room, #north_room, "north"]>, #hotel_area)
assert PassageSide(#passage<[#first_room, #north_room, "north"]>, #first_room, #north_room)
assert PassageSide(#passage<[#first_room, #north_room, "north"]>, #north_room, #first_room)
assert PassageLabel(#passage<[#first_room, #north_room, "north"]>, #first_room, "north")
assert PassageLabel(#passage<[#first_room, #north_room, "north"]>, #north_room, "south")
assert PassageAlias(#passage<[#first_room, #north_room, "north"]>, #first_room, "north")
assert PassageAlias(#passage<[#first_room, #north_room, "north"]>, #first_room, "n")
assert PassageAlias(#passage<[#first_room, #north_room, "north"]>, #north_room, "south")
assert PassageAlias(#passage<[#first_room, #north_room, "north"]>, #north_room, "s")
assert PassageDescription(#passage<[#first_room, #north_room, "north"]>, #first_room, "A northern archway leads deeper into the hotel.")
assert PassageDescription(#passage<[#first_room, #north_room, "north"]>, #north_room, "The way south returns to the first room.")
assert PassageAmbient(#passage<[#first_room, #north_room, "north"]>, #first_room, true)
assert PassageAmbient(#passage<[#first_room, #north_room, "north"]>, #north_room, false)
assert PassageOpen(#passage<[#first_room, #north_room, "north"]>, true)

assert PassageInArea(#passage<[#north_room, #attic, "up"]>, #hotel_area)
assert PassageSide(#passage<[#north_room, #attic, "up"]>, #north_room, #attic)
assert PassageSide(#passage<[#north_room, #attic, "up"]>, #attic, #north_room)
assert PassageLabel(#passage<[#north_room, #attic, "up"]>, #north_room, "up")
assert PassageLabel(#passage<[#north_room, #attic, "up"]>, #attic, "down")
assert PassageAlias(#passage<[#north_room, #attic, "up"]>, #north_room, "up")
assert PassageAlias(#passage<[#north_room, #attic, "up"]>, #north_room, "u")
assert PassageAlias(#passage<[#north_room, #attic, "up"]>, #attic, "down")
assert PassageAlias(#passage<[#north_room, #attic, "up"]>, #attic, "d")
assert PassageDescription(#passage<[#north_room, #attic, "up"]>, #north_room, "A narrow stair climbs into the attic.")
assert PassageDescription(#passage<[#north_room, #attic, "up"]>, #attic, "The stair descends to the north room.")
assert PassageAmbient(#passage<[#north_room, #attic, "up"]>, #north_room, true)
assert PassageAmbient(#passage<[#north_room, #attic, "up"]>, #attic, false)
assert PassageOpen(#passage<[#north_room, #attic, "up"]>, true)

assert command/VerbAlias("l", :look)
assert command/VerbAlias("look", :look)
assert command/VerbAlias("@build", :build_room_command)
assert command/VerbAlias("@dig", :create_passage_command)
assert command/VerbAlias("@tunnel", :create_passage_command)
assert command/VerbAlias("@undig", :remove_passage_command)
assert command/VerbAlias("@remove-exit", :remove_passage_command)
assert command/VerbAlias("@delete-passage", :remove_passage_command)
assert command/VerbAlias("@describe", :set_passage_description_command)
assert command/VerbAlias("@name", :set_entity_name_command)
assert command/VerbAlias("@alias", :set_entity_aliases_command)
assert command/VerbAlias("@portable", :set_entity_portable_command)
assert command/VerbAlias("@fixed", :set_entity_fixed_command)
assert command/VerbAlias("@show", :show_entity_here_command)
assert command/VerbAlias("@hide", :hide_entity_here_command)
assert command/VerbAlias("get", :get)
assert command/VerbAlias("take", :get)
assert command/VerbAlias("drop", :drop)
assert command/VerbAlias("put", :put)
assert command/VerbAlias("push", :push)
assert command/VerbAlias("press", :push)
assert command/VerbAlias("say", :say)
assert command/VerbAlias("emote", :emote)
assert command/PrefixShortcut("\"", :say)
assert command/PrefixShortcut(":", :emote)
assert command/VerbAlias("nod", :nod)
assert command/VerbAlias("wave", :wave)
assert command/VerbAlias("bow", :bow)
assert command/VerbAlias("bonk", :bonk)
assert command/VerbAlias("oif", :oif)
assert command/VerbAlias("smile", :smile)
assert command/VerbAlias("frown", :frown)
assert command/VerbAlias("laugh", :laugh)
assert command/VerbAlias("dance", :dance)
assert command/VerbAlias("shrug", :shrug)
assert command/VerbAlias("think", :think)
assert command/VerbAlias("ponder", :ponder)
assert command/VerbAlias("a", :clap)
assert command/VerbAlias("ap", :clap)
assert command/VerbAlias("app", :clap)
assert command/VerbAlias("appl", :clap)
assert command/VerbAlias("appla", :clap)
assert command/VerbAlias("applau", :clap)
assert command/VerbAlias("applaud", :clap)
assert command/VerbAlias("c", :clap)
assert command/VerbAlias("cl", :clap)
assert command/VerbAlias("cla", :clap)
assert command/VerbAlias("clap", :clap)
assert command/VerbAlias("cheer", :cheer)
assert command/VerbAlias("sigh", :sigh)
assert command/VerbAlias("yawn", :yawn)
assert command/VerbAlias("stretch", :stretch)

assert command/Summary(:look, "inspect the room or a visible thing")
assert command/Summary(:build_room_command, "create a room")
assert command/Summary(:create_passage_command, "connect rooms with a passage")
assert command/Summary(:remove_passage_command, "remove a passage")
assert command/Summary(:set_passage_description_command, "describe rooms, things, and passages")
assert command/Summary(:set_entity_name_command, "rename a room or thing")
assert command/Summary(:set_entity_aliases_command, "set parser aliases for a room or thing")
assert command/Summary(:set_entity_portable_command, "make a thing portable")
assert command/Summary(:set_entity_fixed_command, "make a thing fixed")
assert command/Summary(:show_entity_here_command, "place a thing in this room")
assert command/Summary(:hide_entity_here_command, "unplace a thing from this room")
assert command/Summary(:get, "take a visible thing")
assert command/Summary(:drop, "drop something you carry")
assert command/Summary(:put, "put an item in a container")
assert command/Summary(:push, "use a visible control")
assert command/Summary(:say, "speak to the room")
assert command/Summary(:emote, "pose an action")
assert command/Summary(:think, "record a thought")
assert command/Summary(:nod, "make a social gesture")
assert command/Summary(:wave, "make a social gesture")
assert command/Summary(:bow, "make a social gesture")
assert command/Summary(:bonk, "targeted social action")
assert command/Summary(:smile, "make a social gesture")
assert command/Summary(:frown, "make a social gesture")
assert command/Summary(:laugh, "make a social gesture")
assert command/Summary(:dance, "make a social gesture")
assert command/Summary(:shrug, "make a social gesture")
assert command/Summary(:ponder, "make a social gesture")
assert command/Summary(:clap, "make a social gesture")
assert command/Summary(:cheer, "make a social gesture")
assert command/Summary(:sigh, "make a social gesture")
assert command/Summary(:yawn, "make a social gesture")
assert command/Summary(:stretch, "make a social gesture")

assert command/Form(:look)
assert command/Form(:build_room_command)
assert command/Form(:create_passage_command)
assert command/Form(:remove_passage_command)
assert command/Form(:set_passage_description_command)
assert command/Form(:set_entity_name_command)
assert command/Form(:set_entity_aliases_command)
assert command/Form(:set_entity_portable_command)
assert command/Form(:set_entity_fixed_command)
assert command/Form(:show_entity_here_command)
assert command/Form(:hide_entity_here_command)
assert command/Form(:get)
assert command/Form(:drop)
assert command/Form(:put)
assert command/Form(:push)
assert command/Form(:say)
assert command/Form(:emote)
assert command/Form(:nod)
assert command/Form(:wave)
assert command/Form(:bow)
assert command/Form(:bonk)
assert command/Form(:oif)
assert command/Form(:smile)
assert command/Form(:frown)
assert command/Form(:laugh)
assert command/Form(:dance)
assert command/Form(:shrug)
assert command/Form(:think)
assert command/Form(:ponder)
assert command/Form(:clap)
assert command/Form(:cheer)
assert command/Form(:sigh)
assert command/Form(:yawn)
assert command/Form(:stretch)

assert command/Pattern(:look, :look, #command/pattern/look_room)
assert command/PatternPart(#command/pattern/look_room, 0, :eol, none)

assert command/Pattern(:look, :examine, #command/pattern/look_target)
assert command/PatternPart(#command/pattern/look_target, 0, :optional_prep, :at)
assert command/PatternPart(#command/pattern/look_target, 1, :object_role, :target)

assert command/Pattern(:look, :look_in, #command/pattern/look_inside)
assert command/PatternPart(#command/pattern/look_inside, 0, :prep, :inside)
assert command/PatternPart(#command/pattern/look_inside, 1, :object_role, :target)

assert command/Pattern(:build_room_command, :build_room_command, #command/pattern/message_command)
assert command/Pattern(:create_passage_command, :create_passage_command, #command/pattern/message_command)
assert command/Pattern(:remove_passage_command, :remove_passage_command, #command/pattern/message_command)
assert command/Pattern(:set_passage_description_command, :set_passage_description_command, #command/pattern/message_command)
assert command/Pattern(:set_entity_name_command, :set_entity_name_command, #command/pattern/message_command)
assert command/Pattern(:set_entity_aliases_command, :set_entity_aliases_command, #command/pattern/message_command)
assert command/Pattern(:set_entity_portable_command, :set_entity_portable_command, #command/pattern/message_command)
assert command/Pattern(:set_entity_fixed_command, :set_entity_fixed_command, #command/pattern/message_command)
assert command/Pattern(:show_entity_here_command, :show_entity_here_command, #command/pattern/message_command)
assert command/Pattern(:hide_entity_here_command, :hide_entity_here_command, #command/pattern/message_command)
assert command/PatternPart(#command/pattern/message_command, 0, :message, none)

assert command/Pattern(:get, :get, #command/pattern/get)
assert command/PatternPart(#command/pattern/get, 0, :object_role, :item)

assert command/Pattern(:get, :get_from, #command/pattern/get_from)
assert command/PatternPart(#command/pattern/get_from, 0, :object_role, :item)
assert command/PatternPart(#command/pattern/get_from, 1, :prep, :from)
assert command/PatternPart(#command/pattern/get_from, 2, :object_role, :container)

assert command/Pattern(:drop, :drop, #command/pattern/drop)
assert command/PatternPart(#command/pattern/drop, 0, :object_role, :item)

assert command/Pattern(:put, :put, #command/pattern/put)
assert command/PatternPart(#command/pattern/put, 0, :object_role, :item)
assert command/PatternPart(#command/pattern/put, 1, :optional_prep, :inside)
assert command/PatternPart(#command/pattern/put, 2, :object_role, :container)

assert command/Pattern(:push, :push, #command/pattern/push)
assert command/PatternPart(#command/pattern/push, 0, :object_role, :target)

assert command/Pattern(:say, :say, #command/pattern/say)
assert command/PatternPart(#command/pattern/say, 0, :message, none)

assert command/Pattern(:emote, :emote, #command/pattern/emote)
assert command/PatternPart(#command/pattern/emote, 0, :message, none)

assert command/Pattern(:nod, :nod, #command/pattern/social_none)
assert command/Pattern(:nod, :nod_at, #command/pattern/social_at)
assert command/Pattern(:nod, :nod_to, #command/pattern/social_to)
assert command/Pattern(:wave, :wave, #command/pattern/social_none)
assert command/Pattern(:wave, :wave_at, #command/pattern/social_at)
assert command/Pattern(:wave, :wave_to, #command/pattern/social_to)
assert command/Pattern(:bow, :bow, #command/pattern/social_none)
assert command/Pattern(:bow, :bow_at, #command/pattern/social_at)
assert command/Pattern(:bow, :bow_to, #command/pattern/social_to)
assert command/Pattern(:bonk, :bonk, #command/pattern/social_target)
assert command/Pattern(:oif, :oif, #command/pattern/social_none)
assert command/Pattern(:smile, :smile, #command/pattern/social_none)
assert command/Pattern(:smile, :smile_at, #command/pattern/social_at)
assert command/Pattern(:smile, :smile_to, #command/pattern/social_to)
assert command/Pattern(:frown, :frown, #command/pattern/social_none)
assert command/Pattern(:laugh, :laugh, #command/pattern/social_none)
assert command/Pattern(:dance, :dance, #command/pattern/social_none)
assert command/Pattern(:shrug, :shrug, #command/pattern/social_none)
assert command/Pattern(:think, :think, #command/pattern/think)
assert command/Pattern(:ponder, :ponder, #command/pattern/social_none)
assert command/Pattern(:clap, :clap, #command/pattern/social_none)
assert command/Pattern(:cheer, :cheer, #command/pattern/social_none)
assert command/Pattern(:sigh, :sigh, #command/pattern/social_none)
assert command/Pattern(:yawn, :yawn, #command/pattern/social_none)
assert command/Pattern(:stretch, :stretch, #command/pattern/social_none)

assert command/PatternPart(#command/pattern/social_target, 0, :object_role, :target)
assert command/PatternPart(#command/pattern/social_at, 0, :prep, :at)
assert command/PatternPart(#command/pattern/social_at, 1, :object_role, :target)
assert command/PatternPart(#command/pattern/social_to, 0, :prep, :to)
assert command/PatternPart(#command/pattern/social_to, 1, :object_role, :target)
assert command/PatternPart(#command/pattern/social_none, 0, :eol, none)
assert command/PatternPart(#command/pattern/think, 0, :message, none)

assert command/Preposition(:at, "at")
assert command/Preposition(:inside, "in")
assert command/Preposition(:inside, "inside")
assert command/Preposition(:inside, "into")
assert command/Preposition(:from, "from")
assert command/Preposition(:to, "to")

assert command/MissingArgumentMessage(:examine, "Look at what?")
assert command/MissingArgumentMessage(:look_in, "Look in what?")
assert command/MissingArgumentMessage(:get, "Take what?")
assert command/MissingArgumentMessage(:get_from, "Take what?")
assert command/MissingArgumentMessage(:drop, "Drop what?")
assert command/MissingArgumentMessage(:put, "Put what?")
assert command/MissingArgumentMessage(:push, "Push what?")
assert command/MissingArgumentMessage(:say, "Say what?")
assert command/MissingArgumentMessage(:emote, "Emote what?")
assert command/MissingArgumentMessage(:build_room_command, "Build what?")
assert command/MissingArgumentMessage(:create_passage_command, "Dig where?")
assert command/MissingArgumentMessage(:remove_passage_command, "Remove which passage?")
assert command/MissingArgumentMessage(:set_passage_description_command, "Describe what?")
assert command/MissingArgumentMessage(:set_entity_name_command, "Name what?")
assert command/MissingArgumentMessage(:set_entity_aliases_command, "Alias what?")
assert command/MissingArgumentMessage(:set_entity_portable_command, "Make what portable?")
assert command/MissingArgumentMessage(:set_entity_fixed_command, "Make what fixed?")
assert command/MissingArgumentMessage(:show_entity_here_command, "Show what here?")
assert command/MissingArgumentMessage(:hide_entity_here_command, "Hide what?")
assert command/MissingArgumentMessage(:nod_at, "Nod at who?")
assert command/MissingArgumentMessage(:nod_to, "Nod to who?")
assert command/MissingArgumentMessage(:wave_at, "Wave at who?")
assert command/MissingArgumentMessage(:wave_to, "Wave to who?")
assert command/MissingArgumentMessage(:bow_at, "Bow at who?")
assert command/MissingArgumentMessage(:bow_to, "Bow to who?")
assert command/MissingArgumentMessage(:bonk, "Bonk who?")
assert command/MissingArgumentMessage(:smile_at, "Smile at who?")
assert command/MissingArgumentMessage(:smile_to, "Smile to who?")
assert command/MissingArgumentMessage(:think, "Think what?")

assert command/Role(:look, :actor, :identity)
assert command/Role(:examine, :actor, :identity)
assert command/Role(:examine, :target, :thing)
assert command/Role(:look_in, :actor, :identity)
assert command/Role(:look_in, :target, :container)
assert command/Role(:build_room_command, :actor, :identity)
assert command/Role(:build_room_command, :message, :string)
assert command/Role(:create_passage_command, :actor, :identity)
assert command/Role(:create_passage_command, :message, :string)
assert command/Role(:remove_passage_command, :actor, :identity)
assert command/Role(:remove_passage_command, :message, :string)
assert command/Role(:set_passage_description_command, :actor, :identity)
assert command/Role(:set_passage_description_command, :message, :string)
assert command/Role(:set_entity_name_command, :actor, :identity)
assert command/Role(:set_entity_name_command, :message, :string)
assert command/Role(:set_entity_aliases_command, :actor, :identity)
assert command/Role(:set_entity_aliases_command, :message, :string)
assert command/Role(:set_entity_portable_command, :actor, :identity)
assert command/Role(:set_entity_portable_command, :message, :string)
assert command/Role(:set_entity_fixed_command, :actor, :identity)
assert command/Role(:set_entity_fixed_command, :message, :string)
assert command/Role(:show_entity_here_command, :actor, :identity)
assert command/Role(:show_entity_here_command, :message, :string)
assert command/Role(:hide_entity_here_command, :actor, :identity)
assert command/Role(:hide_entity_here_command, :message, :string)
assert command/Role(:get, :actor, :identity)
assert command/Role(:get, :item, :thing)
assert command/Role(:get_from, :actor, :identity)
assert command/Role(:get_from, :item, :thing)
assert command/Role(:get_from, :container, :container)
assert command/Role(:drop, :actor, :identity)
assert command/Role(:drop, :item, :thing)
assert command/Role(:put, :actor, :identity)
assert command/Role(:put, :item, :thing)
assert command/Role(:put, :container, :thing)
assert command/Role(:push, :actor, :identity)
assert command/Role(:push, :target, :thing)
assert command/Role(:say, :actor, :identity)
assert command/Role(:say, :message, :string)
assert command/Role(:emote, :actor, :identity)
assert command/Role(:emote, :message, :string)
assert command/Role(:nod, :actor, :identity)
assert command/Role(:nod_at, :actor, :identity)
assert command/Role(:nod_at, :target, :thing)
assert command/Role(:nod_to, :actor, :identity)
assert command/Role(:nod_to, :target, :thing)
assert command/Role(:wave, :actor, :identity)
assert command/Role(:wave_at, :actor, :identity)
assert command/Role(:wave_at, :target, :thing)
assert command/Role(:wave_to, :actor, :identity)
assert command/Role(:wave_to, :target, :thing)
assert command/Role(:bow, :actor, :identity)
assert command/Role(:bow_at, :actor, :identity)
assert command/Role(:bow_at, :target, :thing)
assert command/Role(:bow_to, :actor, :identity)
assert command/Role(:bow_to, :target, :thing)
assert command/Role(:bonk, :actor, :identity)
assert command/Role(:bonk, :target, :thing)
assert command/Role(:oif, :actor, :identity)
assert command/Role(:smile, :actor, :identity)
assert command/Role(:smile_at, :actor, :identity)
assert command/Role(:smile_at, :target, :thing)
assert command/Role(:smile_to, :actor, :identity)
assert command/Role(:smile_to, :target, :thing)
assert command/Role(:frown, :actor, :identity)
assert command/Role(:laugh, :actor, :identity)
assert command/Role(:dance, :actor, :identity)
assert command/Role(:shrug, :actor, :identity)
assert command/Role(:think, :actor, :identity)
assert command/Role(:think, :message, :string)
assert command/Role(:ponder, :actor, :identity)
assert command/Role(:clap, :actor, :identity)
assert command/Role(:cheer, :actor, :identity)
assert command/Role(:sigh, :actor, :identity)
assert command/Role(:yawn, :actor, :identity)
assert command/Role(:stretch, :actor, :identity)

assert command/Noun(#coin, "coin")
assert command/NounAlias(#coin, "brass coin")
assert command/Noun(#box, "box")
assert command/NounAlias(#box, "wooden box")
assert command/Noun(#red_button, "button")
assert command/NounAlias(#red_button, "red button")
assert command/Noun(#alice, "alice")
assert command/Noun(#bob, "bob")

assert command/TrustedForName(#player, #coin)
assert command/TrustedForName(#player, #box)
assert command/TrustedForName(#player, #red_button)
assert command/TrustedForName(#player, #alice)
assert command/TrustedForName(#player, #bob)
assert command/TrustedForGrammar(#player, #alice)
assert command/TrustedForGrammar(#player, #bob)
assert command/TrustedForGrammar(#player, #first_room)
assert command/TrustedForGrammar(#player, #north_room)
assert command/TrustedForGrammar(#player, #attic)
assert command/TrustedForGrammar(#player, #builder)

grant role #player
  read:
    :Name
    :Delegates
    :Description
    :LocatedIn
    :RoomInArea
    :ProperNoun
    :Plural
    :PronounSubject
    :PronounObject
    :PronounPossessiveAdjective
    :PronounPossessiveNoun
    :PronounReflexive
    :Portable
    :PassageInArea
    :PassageSide
    :PassageLabel
    :PassageAlias
    :PassageDescription
    :PassageAmbient
    :PassageOpen
    :Exit
    :ReachableRoom
    :Builder
    :Within
    :Contents
    :Carrying
    :CanSee
    :PresentActor
    :ButtonArmed
    :command/VerbAlias
    :command/PrefixShortcut
    :command/Form
    :command/Pattern
    :command/PatternPart
    :command/Role
    :command/Preposition
    :command/Summary
    :command/MissingArgumentMessage
    :command/Noun
    :command/NounAlias
    :command/Name
    :VisibleTo
    :event/Delivery
    :event/Seq
    :event/Text
    :event/Source
    :event/NarrativeEntry
    :command/TrustedForName
    :command/NameMatchable
    :command/TrustedForGrammar
    :command/GrammarSource
    :command/HandlerScope
end

grant role #player
  write:
    :LocatedIn
    :ButtonArmed
    :event/Delivery
    :event/Seq
    :event/Text
    :event/Source
    :Name
    :Description
    :RoomInArea
    :Portable
    :PassageInArea
    :PassageSide
    :PassageLabel
    :PassageAlias
    :PassageDescription
    :PassageAmbient
    :PassageOpen
    :command/Noun
    :command/NounAlias
    :command/TrustedForName
    :command/TrustedForGrammar
end

grant role #player
  invoke:
    :command
    :command/match_object
    :command/bind_pattern
    :look
    :examine
    :look_in
    :go
    :passage/label_for
    :passage/describe_from
    :event/from_template
    :event/narrative
    :event/bindings
    :event/template
    :event/render
    :event/literal
    :event/metadata
    :event/with_metadata
    :event/with_label
    :event/with_class
    :event/with_audience
    :event/with_presentation
    :event/with_group
    :event/replacing_group
    :event/with_location
    :event/scene
    :event/scene_from_template
    :event/scene_at
    :emit_room_event
    :emit_room_event_except
    :event/next_seq
    :event/record_source
    :event/notify
    :event/notify_to
    :get
    :get_from
    :drop
    :put
    :push
    :button_resolves
    :say
    :emote
    :social_event
    :social_directed_event
    :social_emit
    :nod
    :nod_at
    :nod_to
    :wave
    :wave_at
    :wave_to
    :bow
    :bow_at
    :bow_to
    :bonk
    :oif
    :smile
    :smile_at
    :smile_to
    :frown
    :laugh
    :dance
    :shrug
    :think
    :ponder
    :clap
    :cheer
    :sigh
    :yawn
    :stretch
    :template/error
    :template/capitalize
    :template/phrase
    :template/symbol_from_token
    :template/binding_token
    :template/compact_binding_token
    :template/pronoun_type
    :template/parse_binding_suffix
    :template/pronoun_token
    :template/verb_token
    :template/split_once
    :template/article_for
    :template/parse_token
    :template/compile
    :template/from_parts
    :template/name
    :template/reference
    :template/self_alt
    :template/article
    :template/pronoun
    :template/conjugation
    :template/binding_name
    :template/binding_value_text
    :template/binding_value
    :template/reference_text
    :template/pronoun_default
    :template/self_pronoun
    :template/pronoun_for
    :template/conjugate
    :template/render_part
    :template/render_value
    :template/render
    :template/literal
    :template/decompile_part
    :template/decompile
    :message_bag/make
    :message_bag/is_message_bag
    :message_bag/entries
    :message_bag/pick
    :message_bag/add
    :message_bag/remove
    :message_bag/set_entry
    :frob
    :frob_delegate
    :frob_value
    :to_literal
    :trim
    :split
    :string_chars
    :string_from_chars
    :string_concat
    :string_join
    :words
    :string_equal_fold
    :string_starts_with
    :string_contains
    :parse_ordinal
    :edit_distance
    :ui/can_build
    :build_room
    :build_room_command
    :create_passage
    :create_passage_command
    :remove_passage
    :remove_passage_command
    :set_passage_description
    :set_passage_side
    :set_passage_description_command
    :set_entity_name
    :set_entity_aliases
    :set_entity_description
    :set_entity_mobility
    :set_entity_visibility
    :set_entity_attributes
    :set_entity_name_command
    :set_entity_aliases_command
    :set_entity_portable_command
    :set_entity_fixed_command
    :show_entity_here_command
    :hide_entity_here_command
    :builder/can_modify
    :builder/current_area
    :builder/entity_aliases
    :builder/parse_aliases
    :builder/resolve_edit_target
    :builder/room_value
    :builder/room_named_in_area
    :builder/passage_value
    :builder/passage_between
    :builder/passage_from
    :builder/opposite_direction
    :builder/parse_dig
    :builder/parse_attribute_edit
end

grant role #player
  effect
end

command/TrustedForName(actor, obj) :-
  Delegates(actor, #player, ?distance),
  command/TrustedForName(#player, obj)

command/TrustedForGrammar(actor, provider) :-
  Delegates(actor, #player, ?distance),
  command/TrustedForGrammar(#player, provider)

Exit(from, command, to) :-
  PassageSide(passage, from, to),
  PassageAlias(passage, from, command),
  PassageOpen(passage, true)

ReachableRoom(area, from, to) :-
  RoomInArea(from, area),
  RoomInArea(to, area),
  Exit(from, command, to)

ReachableRoom(area, from, to) :-
  RoomInArea(from, area),
  RoomInArea(to, area),
  Exit(from, command, middle),
  ReachableRoom(area, middle, to)

Within(item, place) :-
  LocatedIn(item, place)

Within(item, place) :-
  LocatedIn(item, container),
  Within(container, place)

Contents(place, item) :-
  Within(item, place)

Carrying(actor, item) :-
  LocatedIn(item, actor)

CanSee(actor, room) :-
  LocatedIn(actor, room)

CanSee(actor, item) :-
  LocatedIn(actor, room),
  Within(item, room)

CanSee(actor, item) :-
  Carrying(actor, item)

PresentActor(room, actor) :-
  Delegates(actor, #player, depth),
  LocatedIn(actor, room)

command/Name(obj, name) :-
  command/Noun(obj, name)

command/Name(obj, parser_alias) :-
  command/NounAlias(obj, parser_alias)

VisibleTo(actor, obj) :-
  CanSee(actor, obj)

command/NameMatchable(actor, obj) :-
  VisibleTo(actor, obj),
  command/TrustedForName(actor, obj)

command/GrammarSource(actor, provider) :-
  command/TrustedForGrammar(actor, provider)

command/HandlerScope(actor, actor) :-
  command/TrustedForGrammar(actor, actor)

command/HandlerScope(actor, room) :-
  LocatedIn(actor, room)

// The browser narrative is a listener-scoped projection of the durable event
// journal. Including the source makes grouped replacements observable while
// retaining the event's stable sequence and identity for DOM reconciliation.
event/NarrativeEntry(listener, seq, event, source) :-
  event/Delivery(listener, event),
  event/Seq(event, seq),
  event/Source(event, source)

verb event/next_seq(listener @ #player)
  let seq = 0
  for found in event/Delivery(listener, ?event)
    let event_seq = found[:event].event/seq
    if event_seq > seq
      seq = event_seq
    end
  end

  return seq + 1
end

verb event/record_source(listener @ #player, source @ #event<_>)
  let replace_group = event/metadata(source, :replace_group, none)
  if replace_group != none
    let existing = none
    let existing_seq = -1
    for found in event/Delivery(listener, ?event)
      let candidate = found[:event]
      let candidate_source = candidate.event/source
      if event/metadata(candidate_source, :group, none) == replace_group && candidate.event/seq > existing_seq
        existing = candidate
        existing_seq = candidate.event/seq
      end
    end

    if existing != none
      existing.event/source = source
      existing.event/text = event/render(source, listener)
      return existing
    end
  end

  let seq = event/next_seq(listener)
  let event = #event/log_entry<[listener, seq]>
  let text = event/render(source, listener)

  assert event/Delivery(listener, event)
  event.event/seq = seq
  event.event/text = text
  event.event/source = source

  return event
end

verb event/notify(listener @ #player, text @ #string)
  return event/notify_to(listener, listener, text)
end

verb event/notify_to(listener @ #player, endpoint, text @ #string)
  let source = event/scene(#event/utility, "utility", "event-line utility", "{message}", {:actor -> listener, :message -> text})
  event/record_source(listener, source)
  emit(endpoint, event/render(source, listener))
  return true
end

verb emit_room_event(room @ #room, event @ #event<_>)
  let source = event/scene_at(event, room)
  for found in PresentActor(room, ?listener)
    let listener = found[:listener]
    let text = event/render(source, listener)
    event/record_source(listener, source)
    emit(listener, text)
  end

  return true
end

verb emit_room_event_except(room @ #room, skipped @ #identity, event @ #event<_>)
  let source = event/scene_at(event, room)
  for found in PresentActor(room, ?listener)
    let listener = found[:listener]
    if listener != skipped
      let text = event/render(source, listener)
      event/record_source(listener, source)
      emit(listener, text)
    end
  end

  return true
end

verb event/scene(delegate @ #identity, label @ #string, class @ #string, template @ #string, bindings @ #map)
  return event/with_class(event/with_label(event/narrative(delegate, template, bindings), label), class)
end

verb event/scene_from_template(delegate @ #identity, label @ #string, class @ #string, template, bindings @ #map)
  return event/with_class(event/with_label(event/from_template(delegate, template, bindings), label), class)
end

verb event/scene_at(event @ #event<_>, room @ #room)
  let payload = frob_value(event)
  let bindings = index_or(payload, :bindings, none)
  if bindings == none
    bindings = {}
  end
  if index_or(bindings, :location, none) == none
    bindings[:location] = room
    payload[:bindings] = bindings
  end
  payload[:location] = room
  return frob(frob_delegate(event), payload)
end

verb look(actor @ #player)
  let room = actor.locatedIn
  let source = event/scene(#event/look, "look", "event-line utility", "{message}", {:actor -> actor, :location -> room, :message -> room.description})
  event/record_source(actor, source)
  emit(actor, event/render(source, actor))

  for found in LocatedIn(?item, room)
    let item = found[:item]
    let descriptions = Description(item, ?text)
    if item != actor && descriptions
      let exactly {:text -> text} = descriptions
      event/notify(actor, text)
    end
  end

  return true
end

verb examine(actor @ #player, target @ #thing)
  if CanSee(actor, target)
    let source = event/scene(#event/examine, "examine", "event-line utility", "{message}", {:actor -> actor, :target -> target, :message -> target.description})
    event/record_source(actor, source)
    emit(actor, event/render(source, actor))
    return true
  end

  event/notify(actor, "I do not see that.")
  return false
end

verb look_in(actor @ #player, target @ #container)
  if CanSee(actor, target) == false
    event/notify(actor, "I do not see that.")
    return false
  end

  let found_any = false
  for found in LocatedIn(?item, target)
    let item = found[:item]
    let text = item.description
    if text != none
      found_any = true
      event/notify(actor, text)
    end
  end

  if found_any == false
    event/notify(actor, "It is empty.")
  end

  return true
end

verb passage/label_for(passage, room)
  let labels = PassageLabel(passage, room, ?label)
  let label = none
  if labels
    let exactly {:label -> found_label} = labels
    label = found_label
  else
    let aliases = PassageAlias(passage, room, ?alias)
    if aliases
      let exactly {:alias -> alias} = aliases
      label = alias
    end
  end
  if label == none
    label = "somewhere"
  end

  return label
end

verb passage/describe_from(passage, room)
  let exactly {:description -> description} = PassageDescription(passage, room, ?description)
  if description != none
    return description
  end

  return passage/label_for(passage, room)
end

verb go(actor @ #player, direction)
  let room = actor.locatedIn
  let passage_id = none
  let destination = none
  for found in PassageSide(?passage, room, ?candidate)
    let candidate_passage = found[:passage]
    if passage_id == none && PassageAlias(candidate_passage, room, direction) && PassageOpen(candidate_passage, true)
      passage_id = candidate_passage
      destination = found[:candidate]
    end
  end

  if destination == none
    event/notify(actor, "You cannot go that way.")
    return false
  end

  let from_label = passage/label_for(passage_id, room)
  let to_label = passage/label_for(passage_id, destination)
  let from_description = passage/describe_from(passage_id, room)
  let to_description = passage/describe_from(passage_id, destination)
  let bindings = {:actor -> actor, :message -> from_label, :passage -> passage_id}
  :emit_room_event(room: room, event: event/scene(#event/movement, "movement", "event-line movement", "{Actor} {go|goes} {message}.", bindings))
  actor.locatedIn = destination
  bindings[:message] = to_label
  :emit_room_event_except(room: destination, skipped: actor, event: event/scene(#event/arrival, "movement", "event-line movement", "{Actor} {arrive|arrives} from {message}.", bindings))
  event/notify(actor, destination.description)
  return true
end

verb builder/can_modify(actor @ #player)
  if Builder(actor) || Delegates(actor, #builder, ?builder_depth)
    return true
  end

  event/notify(actor, "You need builder permission to do that.")
  return false
end

verb builder/current_area(actor @ #player)
  let room = actor.locatedIn
  let area = room.roomInArea
  if area == none
    event/notify(actor, "This room is not in an area.")
  end

  return area
end

verb builder/room_value(actor @ #player, name @ #string)
  return frob(#room, [actor, lower(trim(name))])
end

verb builder/passage_value(from @ #room, to @ #room, label @ #string)
  return frob(#passage, [from, to, lower(trim(label))])
end

verb builder/room_named_in_area(area @ #area, name @ #string)
  let wanted = lower(trim(name))
  for found in RoomInArea(?room, area)
    let room = found[:room]
    let room_name = room.name
    if room_name != none && lower(room_name) == wanted
      return room
    end
  end

  return none
end

verb builder/passage_between(from @ #room, to @ #room)
  for found in PassageSide(?passage, from, to)
    return found[:passage]
  end

  return none
end

verb builder/passage_from(from @ #room, command @ #string)
  let direction = lower(trim(command))
  for found in PassageAlias(?passage, from, direction)
    if PassageSide(found[:passage], from, ?destination)
      return found[:passage]
    end
  end

  return none
end

verb builder/opposite_direction(direction @ #string)
  let dir = lower(trim(direction))
  if dir == "north" || dir == "n"
    return "south"
  elseif dir == "south" || dir == "s"
    return "north"
  elseif dir == "east" || dir == "e"
    return "west"
  elseif dir == "west" || dir == "w"
    return "east"
  elseif dir == "up" || dir == "u"
    return "down"
  elseif dir == "down" || dir == "d"
    return "up"
  elseif dir == "in" || dir == "i"
    return "out"
  elseif dir == "out" || dir == "o"
    return "in"
  end

  return ""
end

verb build_room(actor @ #player, name @ #string)
  if builder/can_modify(actor) == false
    return false
  end

  let area = builder/current_area(actor)
  if area == none
    return false
  end

  let trimmed = trim(name)
  if trimmed == ""
    event/notify(actor, "Build what?")
    return false
  end

  let existing = builder/room_named_in_area(area, trimmed)
  if existing != none
    event/notify(actor, "That room already exists in this area.")
    return false
  end

  let room = builder/room_value(actor, trimmed)
  room.name = trimmed
  room.description = string_concat(trimmed, ". An empty room awaiting a description.")
  room.roomInArea = area
  assert command/Noun(room, lower(trimmed))
  assert command/TrustedForName(#player, room)
  assert command/TrustedForGrammar(#player, room)
  event/notify(actor, string_concat("Built ", trimmed, "."))
  return room
end

verb build_room_command(actor @ #player, message @ #string)
  return build_room(actor, message)
end

verb builder/parse_dig(spec @ #string)
  let tokens = words(lower(spec))
  if index_or(tokens, 0, none) == none || index_or(tokens, 1, none) != "to"
    return {:ok -> false}
  end

  let direction = tokens[0]
  let target_words = []
  let index = 2
  while index_or(tokens, index, none) != none
    target_words = [@target_words, tokens[index]]
    index = index + 1
  end

  return {:ok -> true, :direction -> direction, :target -> string_join(target_words, " ")}
end

verb create_passage(actor @ #player, from @ #room, to @ #room, label @ #string, return_label)
  if builder/can_modify(actor) == false
    return false
  end

  let from_area = from.roomInArea
  let to_area = to.roomInArea
  if from_area == none || to_area == none || from_area != to_area
    event/notify(actor, "Passages can only connect rooms in the same area.")
    return false
  end

  let existing = builder/passage_between(from, to)
  if existing != none
    event/notify(actor, "Those rooms already have a passage.")
    return false
  end

  let direction = lower(trim(label))
  if direction == ""
    event/notify(actor, "Dig where?")
    return false
  end

  let reverse = ""
  if return_label != none
    reverse = lower(trim(return_label))
    if reverse == ""
      reverse = builder/opposite_direction(direction)
    end
  end

  let passage = builder/passage_value(from, to, direction)
  assert PassageInArea(passage, from_area)
  assert PassageSide(passage, from, to)
  assert PassageLabel(passage, from, direction)
  assert PassageAlias(passage, from, direction)
  assert PassageDescription(passage, from, string_concat("A passage leads ", direction, "."))
  assert PassageAmbient(passage, from, true)
  if reverse != ""
    assert PassageSide(passage, to, from)
    assert PassageLabel(passage, to, reverse)
    assert PassageAlias(passage, to, reverse)
    assert PassageDescription(passage, to, string_concat("A passage leads ", reverse, "."))
    assert PassageAmbient(passage, to, false)
  end
  assert PassageOpen(passage, true)
  event/notify(actor, "Passage created.")
  return passage
end

verb create_passage_command(actor @ #player, message @ #string)
  let parsed = builder/parse_dig(message)
  if parsed[:ok] == false
    event/notify(actor, "Usage: @dig <direction> to <room name>.")
    return false
  end

  let area = builder/current_area(actor)
  if area == none
    return false
  end

  let target = builder/room_named_in_area(area, parsed[:target])
  if target == none
    event/notify(actor, "No room by that name exists in this area.")
    return false
  end

  return create_passage(actor, actor.locatedIn, target, parsed[:direction], "")
end

verb remove_passage(actor @ #player, passage)
  if builder/can_modify(actor) == false
    return false
  end

  retract PassageInArea(passage, _)
  retract PassageSide(passage, _, _)
  retract PassageLabel(passage, _, _)
  retract PassageAlias(passage, _, _)
  retract PassageDescription(passage, _, _)
  retract PassageAmbient(passage, _, _)
  retract PassageOpen(passage, _)
  event/notify(actor, "Passage removed.")
  return true
end

verb remove_passage_command(actor @ #player, message @ #string)
  let passage = builder/passage_from(actor.locatedIn, message)
  if passage == none
    event/notify(actor, "No passage matches that direction.")
    return false
  end

  return remove_passage(actor, passage)
end

verb set_passage_description(actor @ #player, passage, room @ #room, description @ #string, ambient)
  if builder/can_modify(actor) == false
    return false
  end

  if PassageSide(passage, room, ?destination) == false
    event/notify(actor, "That passage is not connected to this room.")
    return false
  end

  retract PassageDescription(passage, room, _)
  retract PassageAmbient(passage, room, _)
  assert PassageDescription(passage, room, description)
  assert PassageAmbient(passage, room, ambient)
  event/notify(actor, "Passage description updated.")
  return true
end

verb set_passage_side(actor @ #player, passage, room @ #room, label @ #string, description @ #string, ambient)
  if builder/can_modify(actor) == false
    return false
  end

  if PassageSide(passage, room, ?destination) == false
    event/notify(actor, "That passage is not connected to this room.")
    return false
  end

  let direction = lower(trim(label))
  if direction == ""
    event/notify(actor, "Direction is required.")
    return false
  end

  retract PassageLabel(passage, room, _)
  retract PassageAlias(passage, room, _)
  retract PassageDescription(passage, room, _)
  retract PassageAmbient(passage, room, _)
  assert PassageLabel(passage, room, direction)
  assert PassageAlias(passage, room, direction)
  assert PassageDescription(passage, room, description)
  assert PassageAmbient(passage, room, ambient)
  event/notify(actor, "Passage updated.")
  return true
end

verb builder/entity_aliases(entity)
  let aliases = []
  for found in command/NounAlias(entity, ?alias)
    aliases = [@aliases, found[:alias]]
  end

  return aliases
end

verb builder/parse_aliases(text @ #string)
  let aliases = []
  let remaining = text
  while true
    let parts = template/split_once(remaining, ",")
    let alias = remaining
    if parts != []
      alias = parts[0]
      remaining = parts[1]
    end

    alias = lower(trim(alias))
    if alias != ""
      aliases = [@aliases, alias]
    end

    if parts == []
      return aliases
    end
  end
end

verb builder/resolve_edit_target(actor @ #player, target @ #string)
  let wanted = lower(trim(target))
  if wanted == ""
    return none
  end

  if wanted == "here" || wanted == "room" || wanted == "this room"
    return actor.locatedIn
  end

  if wanted == "me" || wanted == "self"
    return actor
  end

  let passage = builder/passage_from(actor.locatedIn, wanted)
  if passage != none
    return passage
  end

  let obj = command/match_object(actor, wanted)
  if obj != none
    return obj
  end

  for found in command/Name(?named, wanted)
    let named = found[:named]
    if command/TrustedForName(actor, named)
      return named
    end
  end

  let area = builder/current_area(actor)
  if area != none
    let room = builder/room_named_in_area(area, wanted)
    if room != none
      return room
    end
  end

  return none
end

verb set_entity_description(actor @ #player, entity, description @ #string)
  if builder/can_modify(actor) == false
    return false
  end

  if is_frob(entity) && frob_delegate(entity) == #passage
    event/notify(actor, "Use passage editing for exits.")
    return false
  end

  let text = trim(description)
  if text == ""
    event/notify(actor, "Description is required.")
    return false
  end

  entity.description = text
  event/notify(actor, "Description updated.")
  return true
end

verb set_entity_name(actor @ #player, entity, name @ #string)
  if builder/can_modify(actor) == false
    return false
  end

  if is_frob(entity) && frob_delegate(entity) == #passage
    event/notify(actor, "Use passage editing for exits.")
    return false
  end

  let label = trim(name)
  if label == ""
    event/notify(actor, "Name is required.")
    return false
  end

  entity.name = label
  retract command/Noun(entity, _)
  assert command/Noun(entity, lower(label))
  assert command/TrustedForName(#player, entity)
  event/notify(actor, "Name updated.")
  return true
end

verb set_entity_aliases(actor @ #player, entity, aliases_text @ #string)
  if builder/can_modify(actor) == false
    return false
  end

  if is_frob(entity) && frob_delegate(entity) == #passage
    event/notify(actor, "Use passage editing for exits.")
    return false
  end

  retract command/NounAlias(entity, _)
  let primary = lower(trim(entity.name))
  for alias in builder/parse_aliases(aliases_text)
    if alias != primary
      assert command/NounAlias(entity, alias)
    end
  end
  assert command/TrustedForName(#player, entity)
  event/notify(actor, "Aliases updated.")
  return true
end

verb set_entity_mobility(actor @ #player, entity, portable)
  if builder/can_modify(actor) == false
    return false
  end

  if Delegates(entity, #room, 0) || Delegates(entity, #player, 0) || is_frob(entity) && frob_delegate(entity) == #passage
    return true
  end

  retract Portable(entity)
  if portable
    assert Portable(entity)
  end
  return true
end

verb set_entity_visibility(actor @ #player, entity, visible_here)
  if builder/can_modify(actor) == false
    return false
  end

  if Delegates(entity, #room, 0) || Delegates(entity, #player, 0) || is_frob(entity) && frob_delegate(entity) == #passage
    return true
  end

  let room = actor.locatedIn
  if room == none
    return false
  end

  if visible_here
    entity.locatedIn = room
  elseif entity.locatedIn == room
    retract LocatedIn(entity, _)
  end
  return true
end

verb set_entity_attributes(actor @ #player, entity, name @ #string, aliases @ #string, description @ #string, portable, visible_here)
  if set_entity_name(actor, entity, name) == false
    return false
  end
  if set_entity_aliases(actor, entity, aliases) == false
    return false
  end
  if set_entity_description(actor, entity, description) == false
    return false
  end
  if set_entity_mobility(actor, entity, portable) == false
    return false
  end
  return set_entity_visibility(actor, entity, visible_here)
end

verb builder/parse_attribute_edit(spec @ #string)
  let parts = template/split_once(spec, " as ")
  if parts == []
    parts = template/split_once(spec, " = ")
  end
  if parts == []
    return {:ok -> false}
  end

  let target = trim(parts[0])
  let value = trim(parts[1])
  return {:ok -> target != "" && value != "", :target -> target, :value -> value}
end

verb set_passage_description_command(actor @ #player, message @ #string)
  let parsed = builder/parse_attribute_edit(message)
  if parsed[:ok] == false
    event/notify(actor, "Usage: @describe <target> as <description>.")
    return false
  end

  let target = builder/resolve_edit_target(actor, parsed[:target])
  if target == none
    event/notify(actor, "No room, thing, or passage matches that target.")
    return false
  end

  if is_frob(target) && frob_delegate(target) == #passage
    return set_passage_description(actor, target, actor.locatedIn, parsed[:value], true)
  end

  return set_entity_description(actor, target, parsed[:value])
end

verb set_entity_name_command(actor @ #player, message @ #string)
  let parsed = builder/parse_attribute_edit(message)
  if parsed[:ok] == false
    event/notify(actor, "Usage: @name <target> as <name>.")
    return false
  end

  let target = builder/resolve_edit_target(actor, parsed[:target])
  if target == none || is_frob(target) && frob_delegate(target) == #passage
    event/notify(actor, "No room or thing matches that target.")
    return false
  end

  return set_entity_name(actor, target, parsed[:value])
end

verb set_entity_aliases_command(actor @ #player, message @ #string)
  let parsed = builder/parse_attribute_edit(message)
  if parsed[:ok] == false
    event/notify(actor, "Usage: @alias <target> as <alias, alias>.")
    return false
  end

  let target = builder/resolve_edit_target(actor, parsed[:target])
  if target == none || is_frob(target) && frob_delegate(target) == #passage
    event/notify(actor, "No room or thing matches that target.")
    return false
  end

  return set_entity_aliases(actor, target, parsed[:value])
end

verb set_entity_portable_command(actor @ #player, message @ #string)
  let target = builder/resolve_edit_target(actor, message)
  if target == none || Delegates(target, #room, 0) || Delegates(target, #player, 0) || is_frob(target) && frob_delegate(target) == #passage
    event/notify(actor, "No thing matches that target.")
    return false
  end

  return set_entity_mobility(actor, target, true)
end

verb set_entity_fixed_command(actor @ #player, message @ #string)
  let target = builder/resolve_edit_target(actor, message)
  if target == none || Delegates(target, #room, 0) || Delegates(target, #player, 0) || is_frob(target) && frob_delegate(target) == #passage
    event/notify(actor, "No thing matches that target.")
    return false
  end

  return set_entity_mobility(actor, target, false)
end

verb show_entity_here_command(actor @ #player, message @ #string)
  let target = builder/resolve_edit_target(actor, message)
  if target == none || Delegates(target, #room, 0) || Delegates(target, #player, 0) || is_frob(target) && frob_delegate(target) == #passage
    event/notify(actor, "No thing matches that target.")
    return false
  end

  return set_entity_visibility(actor, target, true)
end

verb hide_entity_here_command(actor @ #player, message @ #string)
  let target = builder/resolve_edit_target(actor, message)
  if target == none || Delegates(target, #room, 0) || Delegates(target, #player, 0) || is_frob(target) && frob_delegate(target) == #passage
    event/notify(actor, "No thing matches that target.")
    return false
  end

  return set_entity_visibility(actor, target, false)
end

verb get(actor @ #player, item @ #thing)
  if Carrying(actor, item)
    event/notify(actor, "You already have that.")
    return false
  end

  if Portable(item) && CanSee(actor, item)
    let room = actor.locatedIn
    item.locatedIn = actor
    :emit_room_event(room: room, event: event/scene(#event/take, "transfer", "event-line transfer", "{Actor} {take|takes} {the item}.", {:actor -> actor, :item -> item}))
    return true
  end

  event/notify(actor, "You cannot take that.")
  return false
end

verb get_from(actor @ #player, item @ #thing, container @ #container)
  if Carrying(actor, item)
    event/notify(actor, "You already have that.")
    return false
  end

  if CanSee(actor, container) == false
    event/notify(actor, "I do not see that.")
    return false
  end

  if LocatedIn(item, container) && Portable(item)
    let room = actor.locatedIn
    item.locatedIn = actor
    :emit_room_event(room: room, event: event/scene(#event/take_from, "transfer", "event-line transfer", "{Actor} {take|takes} {the item} from {the container}.", {:actor -> actor, :item -> item, :container -> container}))
    return true
  end

  event/notify(actor, "That is not in there.")
  return false
end

verb drop(actor @ #player, item @ #thing)
  let room = actor.locatedIn

  if Carrying(actor, item)
    item.locatedIn = room
    :emit_room_event(room: room, event: event/scene(#event/drop, "transfer", "event-line transfer", "{Actor} {drop|drops} {the item}.", {:actor -> actor, :item -> item}))
    return true
  end

  event/notify(actor, "You are not carrying that.")
  return false
end

verb put(actor @ #player, item @ #thing, container @ #container)
  if Carrying(actor, item) && CanSee(actor, container)
    let room = actor.locatedIn
    item.locatedIn = container
    :emit_room_event(room: room, event: event/scene(#event/put, "transfer", "event-line transfer", "{Actor} {put|puts} {the item} in {the container}.", {:actor -> actor, :item -> item, :container -> container}))
    return true
  end

  event/notify(actor, "You are not carrying that.")
  return false
end

verb push(actor @ #player, target @ #thing)
  if target != #red_button || CanSee(actor, target) == false
    event/notify(actor, "That does not seem pushable.")
    return false
  end

  if ButtonArmed(target)
    event/notify(actor, "The red button is already humming.")
    return false
  end

  let room = actor.locatedIn
  assert ButtonArmed(target)
  event/notify(actor, "You press the red button. It clicks, then begins to hum.")
  let task = spawn :button_resolves(actor: actor, target: target, room: room) after 0.25
  return task != none
end

verb button_resolves(actor @ #player, target @ #thing, room @ #room)
  if ButtonArmed(target) == false
    return false
  end

  retract ButtonArmed(target)
  :emit_room_event(room: room, event: event/scene(#event/button, "alert", "event-line alert", "The red button gives one final cheerful ding.", {:target -> target}))
  return true
end

verb say(actor @ #player, message)
  let room = actor.locatedIn
  let event = event/with_presentation(event/scene(#event/say, "speech", "event-line speech", "{Actor} {say|says}, \"{message}\"", {:actor -> actor, :message -> message}), :speech)
  :emit_room_event(room: room, event: event)
  return true
end

verb emote(actor @ #player, message)
  let room = actor.locatedIn
  let template = template/from_parts([template/name(:emote_actor, true), " ", message])
  let event = event/with_presentation(event/scene_from_template(#event/emote, "emote", "event-line social", template, {:actor -> actor, :emote_actor -> actor.name}), :social)
  :emit_room_event(room: room, event: event)
  return true
end

verb social_event(actor @ #player, template @ #string, bindings @ #map)
  return event/with_presentation(event/scene(#event/social, "social", "event-line social", template, bindings), :social)
end

verb social_emit(actor @ #player, template @ #string, bindings @ #map)
  :emit_room_event(room: actor.locatedIn, event: social_event(actor, template, bindings))
  return true
end

verb social_directed_event(actor @ #player, target @ #thing, prep @ #string, verb_self @ #string, verb_other @ #string)
  return social_event(actor, string_concat("{Actor} {", verb_self, "|", verb_other, "} ", prep, " {the iobj}."), {:actor -> actor, :iobj -> target})
end

verb nod(actor @ #player)
  return social_emit(actor, "{Actor} {nod|nods}.", {:actor -> actor})
end

verb nod_at(actor @ #player, target @ #thing)
  :emit_room_event(room: actor.locatedIn, event: social_directed_event(actor, target, "at", "nod", "nods"))
  return true
end

verb nod_to(actor @ #player, target @ #thing)
  :emit_room_event(room: actor.locatedIn, event: social_directed_event(actor, target, "to", "nod", "nods"))
  return true
end

verb wave(actor @ #player)
  return social_emit(actor, "{Actor} {wave|waves}.", {:actor -> actor})
end

verb wave_at(actor @ #player, target @ #thing)
  :emit_room_event(room: actor.locatedIn, event: social_directed_event(actor, target, "at", "wave", "waves"))
  return true
end

verb wave_to(actor @ #player, target @ #thing)
  :emit_room_event(room: actor.locatedIn, event: social_directed_event(actor, target, "to", "wave", "waves"))
  return true
end

verb bow(actor @ #player)
  return social_emit(actor, "{Actor} {bow|bows}.", {:actor -> actor})
end

verb bow_at(actor @ #player, target @ #thing)
  :emit_room_event(room: actor.locatedIn, event: social_directed_event(actor, target, "at", "bow", "bows"))
  return true
end

verb bow_to(actor @ #player, target @ #thing)
  :emit_room_event(room: actor.locatedIn, event: social_directed_event(actor, target, "to", "bow", "bows"))
  return true
end

verb bonk(actor @ #player, target @ #thing)
  if target == actor
    return social_emit(actor, "{Actor} {bonk|bonks} {r}.", {:actor -> actor})
  end

  return social_emit(actor, "{Actor} {bonk|bonks} {the target}.", {:actor -> actor, :target -> target})
end

verb oif(actor @ #player)
  return say(actor, "Oif!")
end

verb smile(actor @ #player)
  return social_emit(actor, "{Actor} {smile|smiles}.", {:actor -> actor})
end

verb smile_at(actor @ #player, target @ #thing)
  :emit_room_event(room: actor.locatedIn, event: social_directed_event(actor, target, "at", "smile", "smiles"))
  return true
end

verb smile_to(actor @ #player, target @ #thing)
  :emit_room_event(room: actor.locatedIn, event: social_directed_event(actor, target, "to", "smile", "smiles"))
  return true
end

verb frown(actor @ #player)
  return social_emit(actor, "{Actor} {frown|frowns}.", {:actor -> actor})
end

verb laugh(actor @ #player)
  return social_emit(actor, "{Actor} {laugh|laughs}.", {:actor -> actor})
end

verb dance(actor @ #player)
  return social_emit(actor, "{Actor} {dance|dances}.", {:actor -> actor})
end

verb shrug(actor @ #player)
  return social_emit(actor, "{Actor} {shrug|shrugs} {p} shoulders.", {:actor -> actor})
end

verb think(actor @ #player, message)
  return social_emit(actor, "{Actor} . o O ( {message} )", {:actor -> actor, :message -> message})
end

verb ponder(actor @ #player)
  return social_emit(actor, "{Actor} {ponder|ponders}.", {:actor -> actor})
end

verb clap(actor @ #player)
  return social_emit(actor, "{Actor} {clap|claps} {p} hands.", {:actor -> actor})
end

verb cheer(actor @ #player)
  return social_emit(actor, "{Actor} {cheer|cheers}.", {:actor -> actor})
end

verb sigh(actor @ #player)
  return social_emit(actor, "{Actor} {sigh|sighs}.", {:actor -> actor})
end

verb yawn(actor @ #player)
  return social_emit(actor, "{Actor} {yawn|yawns}.", {:actor -> actor})
end

verb stretch(actor @ #player)
  return social_emit(actor, "{Actor} {stretch|stretches}.", {:actor -> actor})
end
